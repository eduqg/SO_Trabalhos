#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int square_number(int number)
{
 	return(sqrt(5));
}

int* vector_values(int number, int initial_number);
{
	int *vector = (int *) calloc(initial_number - number, sizeof(int));

	for(int i = number; i < (initial_number); ++i)
		v[i] =  i;

	return vector;
}

int least_value(int *vector)
{
	return vector[0];
}


int* remove_values(int *vector, int initial_number, int number)
{
	for(int i = number; i <  initial_number; ++i)
	{	
		if(i % number  == 0)
			

	}		
}